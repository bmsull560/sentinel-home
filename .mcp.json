{
  "mcpServers": {
    "repowise": {
      "command": "repowise",
      "args": ["mcp", "--transport", "stdio"],
      "description": "repowise: codebase intelligence — docs, dependency graph, git signals, code health, dead code, decisions"
    }
  }
}
