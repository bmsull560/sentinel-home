apiVersion: v1
kind: ServiceAccount
metadata:
  name: sentinel-home
  namespace: sentinel-home
  annotations:
    # Replace with the IRSA role ARN output from Terraform.
    eks.amazonaws.com/role-arn: "arn:aws:iam::ACCOUNT_ID:role/sentinel-home-ENV-app-irsa"
automountServiceAccountToken: true
